# Handoff

(Empty on purpose — see CLAUDE.md § Plan and status.)

This file is written only when the maintainer explicitly asks for a handoff,
i.e. they are about to clear context and hand back to a fresh session. The
receiving session reads it, folds anything durable into PLAN.md /
SESSION_LOGS.md, and restores this stub. Nothing persists here.
